apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: matchup
spec:
  type: ClusterIP               # Internal only — no external access needed
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
